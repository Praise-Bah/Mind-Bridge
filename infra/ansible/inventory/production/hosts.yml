all:
  children:
    mindbridge:
      hosts:
        mindbridge-prod:
          # Replace with your VPS public IPv4, IPv6, or DNS name.
          ansible_host: 203.0.113.10
          ansible_user: root
          ansible_python_interpreter: /usr/bin/python3
