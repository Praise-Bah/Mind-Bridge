---
name: user-profile
description: What we know about the user — their role, preferences, and how they like to collaborate on MindBridge
metadata:
  type: user
---

Building MindBridge as a solo developer (or very small team). Full-stack capable — comfortable with Django, DRF, React, TypeScript, Redux Toolkit, and Tailwind CSS.

Working on Windows 11 with Python 3.13.2 locally. Backend venv at `backend/venv/`. MySQL is on a remote Ubuntu VPS; local dev uses SQLite.

Prefers to be asked before decisions are made on their behalf (see `[[feedback-ask-before-deciding]]`). Prefers moderate DRY/KISS refactoring with base classes where it saves real repetition — not over-engineering.
